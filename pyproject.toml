[project]
name = "fastapi-app"
version = "0.1.0"
description = "This is a FastAPI app."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115.7",
    "pymongo>=4.10.1",
    "python-dotenv>=1.0.1",
]
